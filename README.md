## Files
- `landing_page.html` — main markup
- `css/styles.css` — styling (Flexbox & Grid)
- `js/script.js` — interactivity: mobile nav, smooth scroll, fade-in on scroll, and client-side form validation

## Tools / Libraries Used
- Plain HTML5, CSS3, Vanilla JavaScript (no frameworks or libraries)
- Inter font assumed (system fallback included). You can add Google Fonts if desired.

## How to run locally
1. Clone the repo:
   ```bash
   https://github.com/Runa-liit/tittan_garage_website.git
   cd titan_garage

## Challenges Faced

Achieving consistent spacing and alignment across multiple sections

Ensuring full responsiveness for mobile, tablet, and desktop

Adjusting images to fit within various grid layouts

Implementing a clean mobile navigation toggle

Handling simple form validation without external libraries